import { fontSize, fontWeight, letterSpacing, lineHeight, textStyles, typography } from "../tokens";

export const fontFamilies = typography;
export const fontSizes = fontSize;
export const lineHeights = lineHeight;
export const fontWeights = fontWeight;
export const letterSpacings = letterSpacing;
export const hierarchy = textStyles;
